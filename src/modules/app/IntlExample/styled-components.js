import styled from "styled-components"

const ExampleSection = styled.div`
    padding: 2rem;
    font-size: 1.2rem;
    && > div:first-child {
        margin-bottom: 1rem;
    }
`

export default {
    ExampleSection,
}
